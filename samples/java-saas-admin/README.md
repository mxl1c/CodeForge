# Java SaaS admin sample (W1 stub)

Minimal enterprise admin slice used by CodeForge's test/QA wedge:

- order approval + tenant isolation
- discount policy
- inventory reserve

This is sample input for `test-gen`, `defect-blame`, and `regress-suggest`.
It is not a runnable product.
